{
  "name": "planckemu",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:codec": "docker run --rm -v $(pwd)/../:/src emscripten/emsdk emmake make",
    "build:bundle": "webpack",
    "build": "npm run build:codec && npm run build:bundle",
    "serve": "http-server",
    "start": "npm run build && npm run serve"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "file-loader": "^6.2.0",
    "webpack": "^5.76.2",
    "webpack-cli": "^5.0.1",
    "webpack-dev-server": "^4.12.0"
  },
  "dependencies": {
    "exports-loader": "^4.0.0",
    "three": "^0.150.1"
  }
}
